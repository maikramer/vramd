[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "vramd"
version = "0.3.0"
description = "VRAM admission control for generative inference on consumer GPUs"
readme = "README.md"
authors = [{ name = "Maikeu Locatelli" }]
requires-python = ">=3.11"
license = "MIT"
keywords = ["gpu", "vram", "inference", "scheduler", "admission-control", "diffusion"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: System :: Distributed Computing",
]

# Deliberately light core: the supervisor doesn't import torch nor create a
# CUDA context. Models live in the workers, each in its own venv.
dependencies = [
    "click>=8.1.0",
    "rich>=13.0.0",
    "rich-click>=1.8.0",
    "PyYAML>=6.0",
    "psutil>=5.9.0",
    "nvidia-ml-py>=12.535.0",
]

[project.optional-dependencies]
dev = ["pytest>=7.4.0", "pytest-cov>=4.0.0", "ruff==0.16.2"]

[project.scripts]
vramd = "vramd.cli:main"

[project.urls]
Homepage = "https://github.com/maikramer/vramd"
Repository = "https://github.com/maikramer/vramd"
Issues = "https://github.com/maikramer/vramd/issues"
Changelog = "https://github.com/maikramer/vramd/blob/main/CHANGELOG.md"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
vramd = ["data/*.yaml", "data/*.glb", "data/*.png", "py.typed"]

[tool.pytest.ini_options]
pythonpath = ["src", "."]
testpaths = ["tests"]
addopts = "--strict-markers"

[tool.coverage.run]
source = ["src/vramd"]
branch = true

[tool.coverage.report]
exclude_also = [
    "if TYPE_CHECKING:",
    "raise NotImplementedError",
    "\\.\\.\\.",
]
